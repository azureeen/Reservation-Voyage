{
  "scripts": {
    "heroku-prebuild": "cd travel-booking && composer install && yarn install && yarn encore production",
    "heroku-postbuild": "cd travel-booking && php bin/console cache:clear"
  }
}
